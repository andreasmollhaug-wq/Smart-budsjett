/** Stabil id — lagres i deliveredAnnouncementIds (user_app_state) så invitasjonen ikke gjentas. */
export const ROADMAP_INVITE_NOTIFICATION_ID = 'roadmap-invite-30m'

/** Akkumulert synlig tid før varsel (30 min). */
export const ROADMAP_INVITE_VISIBLE_MS = 30 * 60 * 1000

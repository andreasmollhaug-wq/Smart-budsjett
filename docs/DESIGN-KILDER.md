# Designkilder

Bruk denne filen til **lenker og filnavn** slik at design og utvikling ikke divergerer.

## Figma / design

- **Figma-URL:** _(ikke satt — legg inn når dere har et offisielt dokument)_
- **Siste oppdatert:** _(dato)_

## Logo og assets

- **Logo / ikon:** SB-badge er implementert i kode (gradient + tekst «SB»). Egne logofiler kan legges i [`public/`](../public/) når de finnes.
- **Skjermbilder til landing:** _(legg til filnavn eller mappe når dere har ekte bilder)_

## Typografi

- **Inter** — lastes via `body` i [`globals.css`](../src/app/globals.css) (system stack som fallback).

## Merknad

Når Figma eller merkevareguide oppdateres, oppdater denne filen og vurder om [`DESIGN.md`](./DESIGN.md) eller `globals.css` må justeres.
